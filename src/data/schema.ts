import { AnySQLiteColumn, integer, real, sqliteTable, text, uniqueIndex, index } from 'drizzle-orm/sqlite-core';
export const habits=sqliteTable('habits',{id:text('id').primaryKey(),parentId:text('parent_id').references(():AnySQLiteColumn=>habits.id,{onDelete:'cascade'}),name:text('name').notNull(),emoji:text('emoji').notNull(),type:text('type',{enum:['boolean','number','duration']}).notNull(),sortOrder:integer('sort_order').notNull(),isGeneral:integer('is_general',{mode:'boolean'}).notNull().default(false),archivedAt:text('archived_at'),createdAt:text('created_at').notNull(),updatedAt:text('updated_at').notNull()},t=>[index('habits_parent_order_idx').on(t.parentId,t.sortOrder)]);
export const entries=sqliteTable('entries',{id:text('id').primaryKey(),habitId:text('habit_id').notNull().references(()=>habits.id,{onDelete:'cascade'}),value:real('value').notNull(),occurredAt:text('occurred_at').notNull(),localDate:text('local_date').notNull(),timezone:text('timezone').notNull(),createdAt:text('created_at').notNull(),updatedAt:text('updated_at').notNull()},t=>[uniqueIndex('entries_habit_day_uq').on(t.habitId,t.localDate),index('entries_date_idx').on(t.localDate)]);
